
  # 💻 Notice Board - React Frontend Client

A decoupled client application built with **React**, **Vite**, and **TailwindCSS** that communicates with the Spring Boot REST API.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  

  Authour : Tanmoy Sarkar